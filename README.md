
  # Recreate Screen

  This is a code bundle for Recreate Screen. The original project is available at https://www.figma.com/design/y2RXqV7jLdDx5MMkfc208R/Recreate-Screen.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  